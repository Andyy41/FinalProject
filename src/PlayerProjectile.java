import java.awt.image.BufferedImage;

public class PlayerProjectile  extends ProjectileBase{
    public PlayerProjectile(double x, double y, double targetX, double targetY, double speed,String image) {
        super(x, y, targetX, targetY, speed, image);
    }


}
